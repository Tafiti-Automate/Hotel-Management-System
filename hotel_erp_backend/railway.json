{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput --settings=core.settings.prod && python manage.py ensure_superuser --settings=core.settings.prod && python manage.py collectstatic --noinput --settings=core.settings.prod && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 3",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
